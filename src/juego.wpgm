import wollok.game.*

object juego{
    method iniciar(){
        game.width(15)
        game.height(10)
        game.cellSize(64)
        game.boardGround("pizarra.jpg")
        game.title("Ahorcado")
        game.start()
    }    
}
